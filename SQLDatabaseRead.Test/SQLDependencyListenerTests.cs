using System;
using Xunit;

namespace SQLDatabaseRead.Test
{
    public class SQLDependencyListenerTests
    {
        [Fact]
        public void Test1()
        {
            string queryString;
            string currentDataQueryString;
            IDataHandler dataHandlerObject;
            string type;
            //SQLDependencyListener sqlDependencyListener = new SQLDependencyListener(queryString, currentDataQueryString, dataHandlerObject, type);
        }
    }
}